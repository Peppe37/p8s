[project]
name = "demo_p8s"
version = "0.1.0"
description = "A P8s demo application showcasing all framework features"
requires-python = ">=3.10"
dependencies = [
    "p8s",
]

[project.optional-dependencies]
ai = [
    "p8s[ai]",
]
dev = [
    "pytest",
    "pytest-asyncio",
    "httpx",
]
